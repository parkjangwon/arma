# ARMA Benchmarks

Use the built-in `stress` binary for end-to-end HTTP measurements. Record throughput, p50, p95 and p99 latency at multiple concurrency levels before changing hot-path synchronization.
